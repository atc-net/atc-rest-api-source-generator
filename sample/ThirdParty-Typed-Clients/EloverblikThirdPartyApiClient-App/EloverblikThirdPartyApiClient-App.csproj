<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>EloverblikThirdPartyApiClientApp</RootNamespace>
    <AssemblyName>EloverblikThirdPartyApiClient-App</AssemblyName>
    <!-- Console demo: literal strings are intentional and not localized. -->
    <NoWarn>$(NoWarn);CA1303</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!--
      TypedClient mode emits a single class taking an HttpClient, so the app only
      needs IHttpClientFactory's typed-client support plus the generic host.
    -->
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Http" Version="10.0.11" />
  </ItemGroup>

  <ItemGroup>
    <!-- The generated client library (generationMode: TypedClient) -->
    <ProjectReference Include="..\Eloverblik.ThirdPartyApi.Client\Eloverblik.ThirdPartyApi.Client.csproj" />
  </ItemGroup>

</Project>
