// <auto-generated />
import type { NotificationType } from '../enums/NotificationType';

export interface CreateSubscriptionRequest {
  readonly name?: string | null;

  /** Event types to subscribe to */
  readonly topics: NotificationType[];

  /** HTTP callback URL (use for server-to-server webhooks) */
  readonly callbackUrl?: string | null;

  /** Subscription duration in minutes (max 30 days) */
  readonly expiresInMinutes?: number | null;
}