// <auto-generated />
import { createContext, useRef, createElement } from 'react';
import type { ReactNode } from 'react';
import { ApiService } from '../client/ApiService';
import type { ApiClientOptions } from '../client/ApiClient';

export interface ApiProviderProps {
  baseUrl: string;
  options?: ApiClientOptions;
  children: ReactNode;
}

export const ApiServiceContext = createContext<ApiService | null>(null);

export function ApiProvider({ baseUrl, options, children }: ApiProviderProps) {
  const serviceRef = useRef<{ service: ApiService; baseUrl: string } | null>(null);
  if (!serviceRef.current || serviceRef.current.baseUrl !== baseUrl) {
    serviceRef.current = { service: new ApiService(baseUrl, options), baseUrl };
  }
  return createElement(ApiServiceContext.Provider, { value: serviceRef.current.service }, children);
}