// <auto-generated />
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseQueryOptions, UseMutationOptions } from '@tanstack/react-query';
import { useApiService } from './useApiService';
import { ApiError } from '../errors/ApiError';
import type { CreateUserRequest, UpdateUserRequest, User, UserQuery, Users } from '../models';

const usersKeys = {
  all: ['users'] as const,
  list: (query?: { search?: string; country?: string; role?: 'Admin' | 'Manager' | 'Employee' | 'Guest'; isActive?: boolean; limit?: number /* default: 25 */ }) => [...usersKeys.all, 'list', query] as const,
  query: (body: UserQuery) => [...usersKeys.all, 'query', body] as const,
  userById: (userId: string) => [...usersKeys.all, 'userById', userId] as const,
};

export { usersKeys };

/** List all users */
export function useListUsers(query?: { search?: string; country?: string; role?: 'Admin' | 'Manager' | 'Employee' | 'Guest'; isActive?: boolean; limit?: number /* default: 25 */ }, options?: Omit<UseQueryOptions<Users, ApiError>, 'queryKey' | 'queryFn'>) {
  const api = useApiService();
  return useQuery({
    queryKey: usersKeys.list(query),
    queryFn: async () => {
      const result = await api.users.listUsers(query);
      if (result.status === 'ok') {
        return result.data;
      }
      throw new ApiError(
        result.response.status,
        result.response.statusText,
        'error' in result ? result.error.message : 'Request failed',
        result.response,
      );
    },
    ...options,
  });
}

/** Query users by a set of ids */
export function useQueryUsers(body: UserQuery, options?: Omit<UseQueryOptions<Users, ApiError>, 'queryKey' | 'queryFn'>) {
  const api = useApiService();
  return useQuery({
    queryKey: usersKeys.query(body),
    queryFn: async () => {
      const result = await api.users.queryUsers(body);
      if (result.status === 'ok') {
        return result.data;
      }
      throw new ApiError(
        result.response.status,
        result.response.statusText,
        'error' in result ? result.error.message : 'Request failed',
        result.response,
      );
    },
    enabled: !!body,
    ...options,
  });
}

/** Create user */
export function useCreateUser(options?: Omit<UseMutationOptions<User, ApiError, CreateUserRequest>, 'mutationFn'>) {
  const api = useApiService();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (body: CreateUserRequest) => {
      const result = await api.users.createUser(body);
      if (result.status === 'created') {
        return result.data;
      }
      throw new ApiError(
        result.response.status,
        result.response.statusText,
        'error' in result ? result.error.message : 'Request failed',
        result.response,
      );
    },
    ...options,
    onSuccess: (data, variables, onMutateResult, context) => {
      queryClient.invalidateQueries({ queryKey: usersKeys.all });
      options?.onSuccess?.(data, variables, onMutateResult, context);
    },
  });
}

/** Get a specific user */
export function useGetUserById(userId: string, options?: Omit<UseQueryOptions<User, ApiError>, 'queryKey' | 'queryFn'>) {
  const api = useApiService();
  return useQuery({
    queryKey: usersKeys.userById(userId),
    queryFn: async () => {
      const result = await api.users.getUserById(userId);
      if (result.status === 'ok') {
        return result.data;
      }
      throw new ApiError(
        result.response.status,
        result.response.statusText,
        'error' in result ? result.error.message : 'Request failed',
        result.response,
      );
    },
    enabled: !!userId,
    ...options,
  });
}

/** Update a specific user */
export function useUpdateUserById(userId: string, options?: Omit<UseMutationOptions<User, ApiError, UpdateUserRequest>, 'mutationFn'>) {
  const api = useApiService();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (body: UpdateUserRequest) => {
      const result = await api.users.updateUserById(userId, body);
      if (result.status === 'ok') {
        return result.data;
      }
      throw new ApiError(
        result.response.status,
        result.response.statusText,
        'error' in result ? result.error.message : 'Request failed',
        result.response,
      );
    },
    ...options,
    onSuccess: (data, variables, onMutateResult, context) => {
      queryClient.invalidateQueries({ queryKey: usersKeys.all });
      options?.onSuccess?.(data, variables, onMutateResult, context);
    },
  });
}

/** Delete a specific user */
export function useDeleteUserById(options?: Omit<UseMutationOptions<void, ApiError, string>, 'mutationFn'>) {
  const api = useApiService();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (userId: string) => {
      const result = await api.users.deleteUserById(userId);
      if (result.status === 'noContent') {
        return;
      }
      throw new ApiError(
        result.response.status,
        result.response.statusText,
        'error' in result ? result.error.message : 'Request failed',
        result.response,
      );
    },
    ...options,
    onSuccess: (data, variables, onMutateResult, context) => {
      queryClient.invalidateQueries({ queryKey: usersKeys.all });
      options?.onSuccess?.(data, variables, onMutateResult, context);
    },
  });
}