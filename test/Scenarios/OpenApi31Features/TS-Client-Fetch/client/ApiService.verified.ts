// <auto-generated />
import { ApiClient, ApiClientOptions } from './ApiClient';
import { ColorsClient } from './ColorsClient';
import { CoordinatesClient } from './CoordinatesClient';
import { DocumentsClient } from './DocumentsClient';

export class ApiService {
  readonly colors: ColorsClient;
  readonly coordinates: CoordinatesClient;
  readonly documents: DocumentsClient;

  constructor(baseUrl: string, options?: ApiClientOptions) {
    const api = new ApiClient(baseUrl, options);
    this.colors = new ColorsClient(api);
    this.coordinates = new CoordinatesClient(api);
    this.documents = new DocumentsClient(api);
  }
}