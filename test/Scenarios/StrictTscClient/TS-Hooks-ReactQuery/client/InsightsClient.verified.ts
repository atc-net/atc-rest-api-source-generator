// <auto-generated />
import type { AxiosResponse } from 'axios';
import { ApiClient } from './ApiClient';
import type { InsightDevice, PaginationResult } from '../models';

export type GetInsightsDevicesPageResult =
  | { status: 'ok'; data: PaginationResult<InsightDevice>; response: AxiosResponse }
  | { status: 'parseError'; error: Error; response: AxiosResponse };

export class InsightsClient {
  private readonly api: ApiClient;

  constructor(api: ApiClient) {
    this.api = api;
  }

  /** Stream insight devices page-by-page (async-enumerable + PaginationResult) */
  async *getInsightsDevices(query?: { pageSize?: number }, headers?: { 'x-continuation'?: string }, signal?: AbortSignal): AsyncGenerator<PaginationResult<InsightDevice>> {
    yield* this.api.requestStream<PaginationResult<InsightDevice>>('GET', '/insights/devices', {
      query: {
        pageSize: query?.pageSize,
      },
      headers: {
        'x-continuation': headers?.['x-continuation'],
      },
      signal,
    });
  }
  /** Stream insight devices page-by-page (async-enumerable + PaginationResult) */

  async getInsightsDevicesPage(query?: { pageSize?: number }, headers?: { 'x-continuation'?: string }): Promise<GetInsightsDevicesPageResult> {
    return this.api.request<PaginationResult<InsightDevice>>('GET', '/insights/devices', {
      query: {
        pageSize: query?.pageSize,
      },
      headers: {
        'x-continuation': headers?.['x-continuation'],
      },
    }) as Promise<GetInsightsDevicesPageResult>;
  }
}