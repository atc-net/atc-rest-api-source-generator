// <auto-generated />
import { ApiClient } from './ApiClient';
import type { Pet } from '../models';
import type { PetId } from '../types/BrandedIds';

export type GetPetResult =
  | { status: 'ok'; data: Pet; response: Response }
  | { status: 'parseError'; error: Error; response: Response };

export class PetsClient {
  private readonly api: ApiClient;

  constructor(api: ApiClient) {
    this.api = api;
  }

  async getPet(petId: PetId): Promise<GetPetResult> {
    return this.api.request<Pet>('GET', `/pets/${petId}`) as Promise<GetPetResult>;
  }
}