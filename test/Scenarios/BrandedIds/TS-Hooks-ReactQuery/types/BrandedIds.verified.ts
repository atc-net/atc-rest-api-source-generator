// <auto-generated />
/**
 * Branded ID types. The intersection with `{ readonly __brand: '...' }` makes
 * each ID nominal at compile time, so `getPet(userId)` is a type error even
 * though both IDs are strings at runtime. Cast with `value as XxxId` when
 * crossing a spec boundary you can't otherwise type — e.g. raw form input.
 */
export type FavoritePetId = string & { readonly __brand: 'FavoritePetId' };
export type OwnerId = string & { readonly __brand: 'OwnerId' };
export type PetId = string & { readonly __brand: 'PetId' };
export type UserId = string & { readonly __brand: 'UserId' };