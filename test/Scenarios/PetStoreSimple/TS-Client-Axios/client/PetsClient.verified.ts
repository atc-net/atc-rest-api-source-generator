// <auto-generated />
import { ApiClient } from './ApiClient';
import type { ApiResult } from '../types/ApiResult';
import type { Pet, Pets } from '../models';

export class PetsClient {
  constructor(private readonly api: ApiClient) {}

  async listPets(query?: { limit?: number }): Promise<ApiResult<Pets>> {
    return this.api.request<Pets>('GET', '/pets', {
      query: {
        limit: query?.limit,
      },
    });
  }

  async createPets(): Promise<ApiResult<void>> {
    return this.api.request<void>('POST', '/pets');
  }

  async showPetById(petId: string): Promise<ApiResult<Pet>> {
    return this.api.request<Pet>('GET', `/pets/${petId}`);
  }
}