// <auto-generated />
namespace ModelsAndProperties.Generated.DependencyInjection;

[GeneratedCode("Atc.Rest.Api.SourceGenerator", "1.0.0")]
public static class ServiceCollectionExtensions
{
    public static IServiceCollection AddModelsAndPropertiesHandlers(this IServiceCollection services)
    {
        // Handler for ICreateAddressHandler must be registered in the Domain project
        // services.AddScoped<ICreateAddressHandler, YourImplementation>();
        // Handler for ICreateModelHandler must be registered in the Domain project
        // services.AddScoped<ICreateModelHandler, YourImplementation>();
        // Handler for IGetArrayTypesHandler must be registered in the Domain project
        // services.AddScoped<IGetArrayTypesHandler, YourImplementation>();
        // Handler for IGetDefaultsHandler must be registered in the Domain project
        // services.AddScoped<IGetDefaultsHandler, YourImplementation>();
        // Handler for IGetDictionaryTypesHandler must be registered in the Domain project
        // services.AddScoped<IGetDictionaryTypesHandler, YourImplementation>();
        // Handler for IGetEnumTypesHandler must be registered in the Domain project
        // services.AddScoped<IGetEnumTypesHandler, YourImplementation>();
        // Handler for IGetModelByIdHandler must be registered in the Domain project
        // services.AddScoped<IGetModelByIdHandler, YourImplementation>();
        // Handler for IGetNullablesHandler must be registered in the Domain project
        // services.AddScoped<IGetNullablesHandler, YourImplementation>();
        // Handler for IGetPrimitiveTypesHandler must be registered in the Domain project
        // services.AddScoped<IGetPrimitiveTypesHandler, YourImplementation>();
        // Handler for IGetStringFormatsHandler must be registered in the Domain project
        // services.AddScoped<IGetStringFormatsHandler, YourImplementation>();
        // Handler for IGetValidationsHandler must be registered in the Domain project
        // services.AddScoped<IGetValidationsHandler, YourImplementation>();
        // Handler for IListAddressesHandler must be registered in the Domain project
        // services.AddScoped<IListAddressesHandler, YourImplementation>();
        // Handler for IListCountriesHandler must be registered in the Domain project
        // services.AddScoped<IListCountriesHandler, YourImplementation>();
        // Handler for IListModelsHandler must be registered in the Domain project
        // services.AddScoped<IListModelsHandler, YourImplementation>();
        // Handler for IListPersonsHandler must be registered in the Domain project
        // services.AddScoped<IListPersonsHandler, YourImplementation>();

        return services;
    }
}