// <auto-generated />
#nullable enable
using System;
using System.Net.Http;
using System.Net.Http.Json;
using System.Threading;
using System.Threading.Tasks;
using Atc.Rest.Client;
using TypedClientResultStyle.Generated.Models;

namespace TypedClientResultStyle.Generated;

[GeneratedCode("Atc.Rest.Api.SourceGenerator", "1.0.0")]
public sealed class DeviceApiClient : IDeviceApiClient
{
    private static readonly JsonSerializerOptions defaultJsonSerializerOptions = new JsonSerializerOptions
    {
        PropertyNameCaseInsensitive = true,
        Converters = { new JsonStringEnumConverter() },
    };

    private readonly HttpClient httpClient;
    private readonly JsonSerializerOptions jsonSerializerOptions;

    public DeviceApiClient(
        HttpClient httpClient)
    {
        this.httpClient = httpClient;
        this.jsonSerializerOptions = defaultJsonSerializerOptions;
    }

    public DeviceApiClient(
        HttpClient httpClient,
        JsonSerializerOptions jsonSerializerOptions)
    {
        this.httpClient = httpClient;
        this.jsonSerializerOptions = jsonSerializerOptions;
    }

    public async Task<EndpointResponse<List<Device>>> ListDevicesAsync(CancellationToken cancellationToken = default)
    {
        var url = "/devices";
        var response = await httpClient.GetAsync(url, cancellationToken);
        return await BuildJsonResponseAsync<List<Device>>(response, jsonSerializerOptions, cancellationToken);
    }

    public async Task<EndpointResponse<Device>> CreateDeviceAsync(
        CreateDeviceParameters parameters,
        CancellationToken cancellationToken = default)
    {
        var url = "/devices";
        var response = await httpClient.PostAsJsonAsync(url, parameters.Request, jsonSerializerOptions, cancellationToken);
        return await BuildJsonResponseAsync<Device>(response, jsonSerializerOptions, cancellationToken);
    }

    public async Task<EndpointResponse<Device>> GetDeviceByIdAsync(
        GetDeviceByIdParameters parameters,
        CancellationToken cancellationToken = default)
    {
        var url = $"/devices/{Uri.EscapeDataString(parameters.DeviceId)}";
        var response = await httpClient.GetAsync(url, cancellationToken);
        return await BuildJsonResponseAsync<Device>(response, jsonSerializerOptions, cancellationToken);
    }

    public async Task<EndpointResponse<Device>> UpdateDeviceAsync(
        UpdateDeviceParameters parameters,
        CancellationToken cancellationToken = default)
    {
        var url = $"/devices/{Uri.EscapeDataString(parameters.DeviceId)}";
        var response = await httpClient.PutAsJsonAsync(url, parameters.Request, jsonSerializerOptions, cancellationToken);
        return await BuildJsonResponseAsync<Device>(response, jsonSerializerOptions, cancellationToken);
    }

    public async Task<EndpointResponse> DeleteDeviceAsync(
        DeleteDeviceParameters parameters,
        CancellationToken cancellationToken = default)
    {
        var url = $"/devices/{Uri.EscapeDataString(parameters.DeviceId)}";
        var response = await httpClient.DeleteAsync(url, cancellationToken);
        return await BuildEmptyResponseAsync(response, cancellationToken);
    }

    public async Task<EndpointResponse<byte[]>> DownloadFirmwareAsync(
        DownloadFirmwareParameters parameters,
        CancellationToken cancellationToken = default)
    {
        var url = $"/devices/{Uri.EscapeDataString(parameters.DeviceId)}/firmware";
        var response = await httpClient.GetAsync(url, cancellationToken);
        return await BuildBinaryResponseAsync(response, cancellationToken);
    }

    public async Task<EndpointResponse<string>> GetDeviceLogAsync(
        GetDeviceLogParameters parameters,
        CancellationToken cancellationToken = default)
    {
        var url = $"/devices/{Uri.EscapeDataString(parameters.DeviceId)}/log";
        var response = await httpClient.GetAsync(url, cancellationToken);
        return await BuildTextResponseAsync(response, cancellationToken);
    }

    private static async System.Threading.Tasks.Task EnsureSuccessAsync(
        HttpResponseMessage response,
        CancellationToken cancellationToken)
    {
        if (response.IsSuccessStatusCode)
        {
            return;
        }

        var errorContent = await response.Content.ReadAsStringAsync(cancellationToken);
        throw new HttpRequestException(
            $"HTTP {(int)response.StatusCode} ({response.ReasonPhrase}): {errorContent}",
            inner: null,
            response.StatusCode);
    }

    private static IReadOnlyDictionary<string, IEnumerable<string>> ReadHeaders(HttpResponseMessage response)
    {
        var headers = new Dictionary<string, IEnumerable<string>>(StringComparer.OrdinalIgnoreCase);

        foreach (var header in response.Headers)
        {
            headers[header.Key] = header.Value;
        }

        foreach (var header in response.Content.Headers)
        {
            headers[header.Key] = header.Value;
        }

        return headers;
    }

    private static async System.Threading.Tasks.Task<EndpointResponse> BuildEmptyResponseAsync(
        HttpResponseMessage response,
        CancellationToken cancellationToken)
    {
        var content = await response.Content.ReadAsStringAsync(cancellationToken);

        return new EndpointResponse(
            response.IsSuccessStatusCode,
            response.StatusCode,
            content,
            contentObject: null,
            ReadHeaders(response));
    }

    private static async System.Threading.Tasks.Task<EndpointResponse<T>> BuildJsonResponseAsync<T>(
        HttpResponseMessage response,
        JsonSerializerOptions jsonSerializerOptions,
        CancellationToken cancellationToken)
    {
        var content = await response.Content.ReadAsStringAsync(cancellationToken);

        object? contentObject = null;
        if (response.IsSuccessStatusCode && !string.IsNullOrEmpty(content))
        {
            contentObject = JsonSerializer.Deserialize<T>(content, jsonSerializerOptions);
        }

        return new EndpointResponse<T>(
            response.IsSuccessStatusCode,
            response.StatusCode,
            content,
            contentObject,
            ReadHeaders(response));
    }

    private static async System.Threading.Tasks.Task<EndpointResponse<byte[]>> BuildBinaryResponseAsync(
        HttpResponseMessage response,
        CancellationToken cancellationToken)
    {
        var bytes = await response.Content.ReadAsByteArrayAsync(cancellationToken);

        return new EndpointResponse<byte[]>(
            response.IsSuccessStatusCode,
            response.StatusCode,
            content: string.Empty,
            response.IsSuccessStatusCode ? bytes : null,
            ReadHeaders(response));
    }

    private static async System.Threading.Tasks.Task<EndpointResponse<string>> BuildTextResponseAsync(
        HttpResponseMessage response,
        CancellationToken cancellationToken)
    {
        var content = await response.Content.ReadAsStringAsync(cancellationToken);

        return new EndpointResponse<string>(
            response.IsSuccessStatusCode,
            response.StatusCode,
            content,
            response.IsSuccessStatusCode ? content : null,
            ReadHeaders(response));
    }

    private static async System.Threading.Tasks.Task<EndpointResponse<Uri>> BuildLocationResponseAsync(
        HttpResponseMessage response,
        CancellationToken cancellationToken)
    {
        var content = await response.Content.ReadAsStringAsync(cancellationToken);

        return new EndpointResponse<Uri>(
            response.IsSuccessStatusCode,
            response.StatusCode,
            content,
            response.Headers.Location,
            ReadHeaders(response));
    }
}