// <auto-generated />
import type { AxiosResponse } from 'axios';
import { ApiClient } from './ApiClient';
import type { IdList, Item } from '../models';

export type ListItemsResult =
  | { status: 'ok'; data: Item[]; response: AxiosResponse }
  | { status: 'parseError'; error: Error; response: AxiosResponse };

export class ItemsClient {
  private readonly api: ApiClient;

  constructor(api: ApiClient) {
    this.api = api;
  }

  /** List items */
  async listItems(query?: { tags?: string[]; q?: string; legacy?: string[]; ids?: IdList }): Promise<ListItemsResult> {
    return this.api.request<Item[]>('GET', '/items', {
      query: {
        tags: query?.tags,
        q: query?.q,
        legacy: query?.legacy,
        ids: query?.ids,
      },
    }) as Promise<ListItemsResult>;
  }
}