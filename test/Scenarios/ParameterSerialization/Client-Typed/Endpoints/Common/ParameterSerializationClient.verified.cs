// <auto-generated />
#nullable enable
using System;
using System.Net.Http;
using System.Net.Http.Json;
using System.Threading;
using System.Threading.Tasks;
using ParameterSerialization.Generated.Models;

namespace ParameterSerialization.Generated.Client;

[GeneratedCode("Atc.Rest.Api.SourceGenerator", "1.0.0")]
public sealed class ParameterSerializationClient
{
    private static readonly JsonSerializerOptions defaultJsonSerializerOptions = new JsonSerializerOptions
    {
        PropertyNameCaseInsensitive = true,
        Converters = { new JsonStringEnumConverter() },
    };

    private readonly HttpClient httpClient;
    private readonly JsonSerializerOptions jsonSerializerOptions;

    public ParameterSerializationClient(
        HttpClient httpClient)
    {
        this.httpClient = httpClient;
        this.jsonSerializerOptions = defaultJsonSerializerOptions;
    }

    public ParameterSerializationClient(
        HttpClient httpClient,
        JsonSerializerOptions jsonSerializerOptions)
    {
        this.httpClient = httpClient;
        this.jsonSerializerOptions = jsonSerializerOptions;
    }

    public async Task<List<Item>> ListItemsAsync(
        ListItemsParameters parameters,
        CancellationToken cancellationToken = default)
    {
        var url = "/items";
        var queryParams = new List<string>();

        if (parameters.Tags is not null)
        {
            foreach (var item in parameters.Tags)
            {
                queryParams.Add($"tags={Uri.EscapeDataString(item)}");
            }
        }

        if (!string.IsNullOrEmpty(parameters.Q))
        {
            queryParams.Add($"q={parameters.Q}");
        }

        if (parameters.Legacy is not null)
        {
            queryParams.Add($"legacy={Uri.EscapeDataString($"{parameters.Legacy}")}");
        }

        if (parameters.Ids is not null)
        {
            foreach (var item in parameters.Ids)
            {
                queryParams.Add($"ids={Uri.EscapeDataString(item)}");
            }
        }

        if (queryParams.Count > 0)
        {
            url += "?" + string.Join("&", queryParams);
        }


        if (!string.IsNullOrEmpty(parameters.RawFilter))
        {
            url += (url.Contains('?') ? "&" : "?") + parameters.RawFilter;
        }


        using var request = new HttpRequestMessage(HttpMethod.Get, url);

        var cookieParts = new List<string>();
        if (!string.IsNullOrEmpty(parameters.Session))
        {
            cookieParts.Add($"session={parameters.Session}");
        }

        if (cookieParts.Count > 0)
        {
            request.Headers.TryAddWithoutValidation("Cookie", string.Join("; ", cookieParts));
        }

        var response = await httpClient.SendAsync(request, cancellationToken);
        await EnsureSuccessAsync(response, cancellationToken);
        return (await response.Content.ReadFromJsonAsync<List<Item>>(jsonSerializerOptions, cancellationToken))!;
    }

    private static async System.Threading.Tasks.Task EnsureSuccessAsync(
        HttpResponseMessage response,
        CancellationToken cancellationToken)
    {
        if (response.IsSuccessStatusCode)
        {
            return;
        }

        var errorContent = await response.Content.ReadAsStringAsync(cancellationToken);
        throw new HttpRequestException(
            $"HTTP {(int)response.StatusCode} ({response.ReasonPhrase}): {errorContent}",
            inner: null,
            response.StatusCode);
    }
}