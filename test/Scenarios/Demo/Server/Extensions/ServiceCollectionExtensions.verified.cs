// <auto-generated />
namespace Demo.Generated.DependencyInjection;

[GeneratedCode("Atc.Rest.Api.SourceGenerator", "1.0.0")]
public static class ServiceCollectionExtensions
{
    public static IServiceCollection AddDemoHandlers(this IServiceCollection services)
    {
        // Handler for ICreateAccountHandler must be registered in the Domain project
        // services.AddScoped<ICreateAccountHandler, YourImplementation>();
        // Handler for ICreateTaskHandler must be registered in the Domain project
        // services.AddScoped<ICreateTaskHandler, YourImplementation>();
        // Handler for ICreateTestItemEmptyHandler must be registered in the Domain project
        // services.AddScoped<ICreateTestItemEmptyHandler, YourImplementation>();
        // Handler for ICreateTestItemWithLocationHeaderHandler must be registered in the Domain project
        // services.AddScoped<ICreateTestItemWithLocationHeaderHandler, YourImplementation>();
        // Handler for ICreateTestItemWithLocationInBodyHandler must be registered in the Domain project
        // services.AddScoped<ICreateTestItemWithLocationInBodyHandler, YourImplementation>();
        // Handler for ICreateTestItemWithModelHandler must be registered in the Domain project
        // services.AddScoped<ICreateTestItemWithModelHandler, YourImplementation>();
        // Handler for ICreateUserHandler must be registered in the Domain project
        // services.AddScoped<ICreateUserHandler, YourImplementation>();
        // Handler for IDeleteAccountByIdHandler must be registered in the Domain project
        // services.AddScoped<IDeleteAccountByIdHandler, YourImplementation>();
        // Handler for IDeleteTaskByIdHandler must be registered in the Domain project
        // services.AddScoped<IDeleteTaskByIdHandler, YourImplementation>();
        // Handler for IDeleteUserByIdHandler must be registered in the Domain project
        // services.AddScoped<IDeleteUserByIdHandler, YourImplementation>();
        // Handler for IGetAccountByIdHandler must be registered in the Domain project
        // services.AddScoped<IGetAccountByIdHandler, YourImplementation>();
        // Handler for IGetFileByIdHandler must be registered in the Domain project
        // services.AddScoped<IGetFileByIdHandler, YourImplementation>();
        // Handler for IGetTaskByIdHandler must be registered in the Domain project
        // services.AddScoped<IGetTaskByIdHandler, YourImplementation>();
        // Handler for IGetUserByIdHandler must be registered in the Domain project
        // services.AddScoped<IGetUserByIdHandler, YourImplementation>();
        // Handler for IListAccountsHandler must be registered in the Domain project
        // services.AddScoped<IListAccountsHandler, YourImplementation>();
        // Handler for IListAsyncEnumerableAccountsHandler must be registered in the Domain project
        // services.AddScoped<IListAsyncEnumerableAccountsHandler, YourImplementation>();
        // Handler for IListPaginatedAccountsHandler must be registered in the Domain project
        // services.AddScoped<IListPaginatedAccountsHandler, YourImplementation>();
        // Handler for IListTasksHandler must be registered in the Domain project
        // services.AddScoped<IListTasksHandler, YourImplementation>();
        // Handler for IListUsersHandler must be registered in the Domain project
        // services.AddScoped<IListUsersHandler, YourImplementation>();
        // Handler for IUpdateAccountByIdHandler must be registered in the Domain project
        // services.AddScoped<IUpdateAccountByIdHandler, YourImplementation>();
        // Handler for IUpdateTaskByIdHandler must be registered in the Domain project
        // services.AddScoped<IUpdateTaskByIdHandler, YourImplementation>();
        // Handler for IUpdateUserByIdHandler must be registered in the Domain project
        // services.AddScoped<IUpdateUserByIdHandler, YourImplementation>();
        // Handler for IUploadFormDataFileHandler must be registered in the Domain project
        // services.AddScoped<IUploadFormDataFileHandler, YourImplementation>();
        // Handler for IUploadFormDataFilesHandler must be registered in the Domain project
        // services.AddScoped<IUploadFormDataFilesHandler, YourImplementation>();
        // Handler for IUploadMultiFilesAsFormDataHandler must be registered in the Domain project
        // services.AddScoped<IUploadMultiFilesAsFormDataHandler, YourImplementation>();
        // Handler for IUploadSingleFileAsFormDataHandler must be registered in the Domain project
        // services.AddScoped<IUploadSingleFileAsFormDataHandler, YourImplementation>();

        return services;
    }
}