// <auto-generated />
namespace PetStoreFull.Generated.DependencyInjection;

[GeneratedCode("Atc.Rest.Api.SourceGenerator", "1.0.0")]
public static class ServiceCollectionExtensions
{
    public static IServiceCollection AddPetStoreFullHandlers(this IServiceCollection services)
    {
        // Handler for IAddPetHandler must be registered in the Domain project
        // services.AddScoped<IAddPetHandler, YourImplementation>();
        // Handler for ICreateUserHandler must be registered in the Domain project
        // services.AddScoped<ICreateUserHandler, YourImplementation>();
        // Handler for ICreateUsersWithListInputHandler must be registered in the Domain project
        // services.AddScoped<ICreateUsersWithListInputHandler, YourImplementation>();
        // Handler for IDeleteOrderHandler must be registered in the Domain project
        // services.AddScoped<IDeleteOrderHandler, YourImplementation>();
        // Handler for IDeletePetHandler must be registered in the Domain project
        // services.AddScoped<IDeletePetHandler, YourImplementation>();
        // Handler for IDeleteUserHandler must be registered in the Domain project
        // services.AddScoped<IDeleteUserHandler, YourImplementation>();
        // Handler for IFindPetsByStatusHandler must be registered in the Domain project
        // services.AddScoped<IFindPetsByStatusHandler, YourImplementation>();
        // Handler for IFindPetsByTagsHandler must be registered in the Domain project
        // services.AddScoped<IFindPetsByTagsHandler, YourImplementation>();
        // Handler for IGetInventoryHandler must be registered in the Domain project
        // services.AddScoped<IGetInventoryHandler, YourImplementation>();
        // Handler for IGetOrderByIdHandler must be registered in the Domain project
        // services.AddScoped<IGetOrderByIdHandler, YourImplementation>();
        // Handler for IGetPetByIdHandler must be registered in the Domain project
        // services.AddScoped<IGetPetByIdHandler, YourImplementation>();
        // Handler for IGetUserByNameHandler must be registered in the Domain project
        // services.AddScoped<IGetUserByNameHandler, YourImplementation>();
        // Handler for ILoginUserHandler must be registered in the Domain project
        // services.AddScoped<ILoginUserHandler, YourImplementation>();
        // Handler for ILogoutUserHandler must be registered in the Domain project
        // services.AddScoped<ILogoutUserHandler, YourImplementation>();
        // Handler for IPlaceOrderHandler must be registered in the Domain project
        // services.AddScoped<IPlaceOrderHandler, YourImplementation>();
        // Handler for IUpdatePetHandler must be registered in the Domain project
        // services.AddScoped<IUpdatePetHandler, YourImplementation>();
        // Handler for IUpdatePetWithFormHandler must be registered in the Domain project
        // services.AddScoped<IUpdatePetWithFormHandler, YourImplementation>();
        // Handler for IUpdateUserHandler must be registered in the Domain project
        // services.AddScoped<IUpdateUserHandler, YourImplementation>();
        // Handler for IUploadFileHandler must be registered in the Domain project
        // services.AddScoped<IUploadFileHandler, YourImplementation>();

        return services;
    }
}