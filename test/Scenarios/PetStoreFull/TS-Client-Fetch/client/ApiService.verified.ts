// <auto-generated />
import { ApiClient, ApiClientOptions } from './ApiClient';
import { PetClient } from './PetClient';
import { StoreClient } from './StoreClient';
import { UserClient } from './UserClient';

export class ApiService {
  readonly pet: PetClient;
  readonly store: StoreClient;
  readonly user: UserClient;

  constructor(baseUrl: string, options?: ApiClientOptions) {
    const api = new ApiClient(baseUrl, options);
    this.pet = new PetClient(api);
    this.store = new StoreClient(api);
    this.user = new UserClient(api);
  }
}