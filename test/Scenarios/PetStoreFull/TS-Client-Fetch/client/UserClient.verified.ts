// <auto-generated />
import { ApiClient } from './ApiClient';
import type { ApiResult } from '../types/ApiResult';
import type { User } from '../models';

export class UserClient {
  constructor(private readonly api: ApiClient) {}

  async createUser(body: User): Promise<ApiResult<User>> {
    return this.api.request<User>('POST', '/user', {
      body,
    });
  }

  async createUsersWithListInput(body: User[]): Promise<ApiResult<User>> {
    return this.api.request<User>('POST', '/user/createWithList', {
      body,
    });
  }

  async loginUser(query?: { username?: string; password?: string }): Promise<ApiResult<string>> {
    return this.api.request<string>('GET', '/user/login', {
      query: {
        username: query?.username,
        password: query?.password,
      },
    });
  }

  async logoutUser(): Promise<ApiResult<void>> {
    return this.api.request<void>('GET', '/user/logout');
  }

  async getUserByName(username: string): Promise<ApiResult<User>> {
    return this.api.request<User>('GET', `/user/${username}`);
  }

  async updateUser(username: string, body: User): Promise<ApiResult<void>> {
    return this.api.request<void>('PUT', `/user/${username}`, {
      body,
    });
  }

  async deleteUser(username: string): Promise<ApiResult<void>> {
    return this.api.request<void>('DELETE', `/user/${username}`);
  }
}