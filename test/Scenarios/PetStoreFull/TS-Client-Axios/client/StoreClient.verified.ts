// <auto-generated />
import { ApiClient } from './ApiClient';
import type { ApiResult } from '../types/ApiResult';
import type { Order } from '../models';

export class StoreClient {
  constructor(private readonly api: ApiClient) {}

  async getInventory(): Promise<ApiResult<unknown>> {
    return this.api.request<unknown>('GET', '/store/inventory');
  }

  async placeOrder(body: Order): Promise<ApiResult<Order>> {
    return this.api.request<Order>('POST', '/store/order', {
      body,
    });
  }

  async getOrderById(orderId: number): Promise<ApiResult<Order>> {
    return this.api.request<Order>('GET', `/store/order/${orderId}`);
  }

  async deleteOrder(orderId: number): Promise<ApiResult<void>> {
    return this.api.request<void>('DELETE', `/store/order/${orderId}`);
  }
}